<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <h2><%= post.title %></h2>
      <%= post.body %>
      <br>
      <br>
    </li>
  <% end %>
</ul>

<%= link_to "New Post", new_post_path %>
<%= button_to "logout", destroy_user_session_path, method: :delete  %>